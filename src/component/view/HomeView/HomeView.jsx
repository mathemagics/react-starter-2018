import React from 'react';

// Import components =============================================================
import Button from 'component/base/Button';

const HomeView = () => (
  <Button onClick={() => {}}>
    Home
  </Button>
);

export default HomeView;
